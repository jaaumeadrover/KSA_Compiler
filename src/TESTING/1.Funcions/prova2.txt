func int sumarNombresArray(int array a, int b, int longitud){
    const int final<-3;
    const bool tre<-true;
    const bool treu<-false;

    return b;
}

main(){
    //DECLARACIONS
    int l <- 10;
    int acum <- 0;
    int a <- int[10];
    //a<-3;
    for(int i <- 0; i < l; i <- i + 1){
        a[i] <- 1;
    }
    //Cridem la funcio que ens sumarà els nombres de l'array
    acum <- sumarNombresArray(a,acum,l);
    l<-sumarNombresArray(a,acum,l);
}